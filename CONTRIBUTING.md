# 维护与贡献

请在本 Git 仓库内开发，保持一份源码；不要通过复制整个项目或叠加旧 ZIP 维护版本。安装方法见 [README](README.md)。

## 目录职责

| 路径 | 用途 |
|---|---|
| `browser-to-tool/SKILL.md` | 助手加载的主流程与使用边界 |
| `browser-to-tool/references/` | 按需读取的客户端、环境、浏览器、HTTP 和交付说明 |
| `browser-to-tool/scripts/` | 桌面任务目录脚本及配套浏览器运行器 |
| `browser-to-tool/package.json`、`package-lock.json` | 配套运行器依赖及锁定版本 |
| `维护者/` | 自检、合成回归及待运行模型评测场景 |
| `docs/` | 设计依据、调研结论和已测/未测范围 |

安装单位是完整的 `browser-to-tool/`，不是整个仓库，也不是单个 `SKILL.md`。本机 `node_modules/`、账户、登录资料、业务数据和临时输出不进入提交或分享包；忽略规则见 [.gitignore](.gitignore)。

## 修改与检查

保持改动聚焦。修改流程时同步受影响的参考文档；修改运行器行为时补充对应回归。保留现有目录名，避免无必要的重命名破坏安装说明和脚本引用。

在仓库根目录运行基础检查：

```sh
python3 维护者/check.py
```

它检查文档引用、策略文本、脚本语法和临时目录行为，不证明真实模型或网站已通过验收。

修改浏览器运行器时，在依赖及专用 Chromium 已准备好的环境运行：

```sh
node --test 维护者/browser*.test.mjs
```

缺依赖时先按[运行说明](browser-to-tool/references/playwright-runtime.md)准备。测试使用合成网站及隔离 profile，不使用个人浏览器资料或真实业务账户。环境、可见窗口检查和覆盖边界见[验证记录](docs/playwright-validation.md)。

提交前查看 `git diff --check`、`git diff` 和 `git status --short`，确认没有混入凭据、业务结果或临时文件。变更说明写清问题、修改后的行为、实际运行的检查和未测范围。`维护者/evals.json` 是评测规格，不是通过报告；历史检查数量也不能替代本次结果。

## 对外更新

功能限制和用户操作方式变化时更新 README；验证记录注明日期和环境。分发使用 GitHub 上已确认的版本，不把本地历史备份当最新安装包。更新安装副本时替换完整 Skill，并按 README 处理已有版本和运行中的会话。

## 非开发同事试点

先选一个已有客户端、一种电脑环境和一个可人工核对的只读任务，邀请三位同事。维护者协助首次环境准备后，让同事仅凭 README 和工具使用说明操作；不预先教接口、分页或控制台。Windows 配套运行器仍禁用，选择环境时先确认实际可用通道。

开发交付后，在不使用编码助手的条件下，依次观察同事从成品入口首次取得结果、隔天改日期再次运行、登录失效后的恢复。检查运行不依赖开发期临时连接或助手进程；仍需浏览器或本地运行时可以明确保留。对应模型评测规格是 `维护者/evals.json` 的21—23项；先用合成环境检查，再在授权范围内做真人试用，二者分别记录。登录失效场景使用测试账户或合成环境，不为了测试撤销个人账户会话。

每次记录：客户端与系统、任务范围、总耗时（含安装和排错）、向维护者求助次数、数据核对结果、是否独立完成、失败原因。结果应能区分首次制作成本与后续节省的时间。没有实测时保留“待试用”，不以文档或合成检查通过宣称普通同事已能独立使用。

24—25项补充从空任务开始的交付与首次联网故障；不要只给模型一个已经做好的工具检查。核对启动文件在无关工作目录下运行、必需字段有实际值、重复运行保留旧文件、输出能定位；证书或网络修复后重跑实际入口。保留合成测试、真实网站验证和同事独立操作三个层次的记录。试用复盘只提交去标识的问题摘要，不提交共享对话全文、同事个人路径或业务数据。

26—28项补充权限恢复后的持续执行、列表与详情的完整样本导出、部分结果和逐字段恢复，以及当前调查工具与独立成品的能力边界。记录实际加载的Skill来源/摘要值，避免只凭文件数量判断更新成功；测试中用户暂停或缩小范围时，以其最新指令为准。

29项验证页面标题与导出语言一致，覆盖接口默认语言、详情覆盖列表、旧缓存及中英混合标题。读取实际文件逐值对照来源；设置语言参数和检测到汉字都不能代替验证，机器翻译不能用来冒充采到约定版本。
